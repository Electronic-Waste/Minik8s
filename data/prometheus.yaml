kind: Pod
metadata:
  name: monitor
spec:
  volumes:
  - name: prometheus
    hostPath: /opt/prometheus

  containers:
  - name: prometheus
    image: docker.io/prom/prometheus:latest
    volumeMounts:
      - name: prometheus
        mountPath: /etc/prometheus
    ports:
      - containerPort: 9090