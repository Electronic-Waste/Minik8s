kind: Pod
metadata:
  name: apiserver
spec:
  volumes:
  - name: shared-data
    hostPath: /root/minik8s/minik8s/bin

  containers:

  - name: api
    image: docker.io/library/golang:latest
    resources:
      limits:
        memory: "500MiB"
      requests:
        memory: "200MiB"
    volumeMounts:
      - name: shared-data
        mountPath: /mnt
    ports:
      - containerPort: 8080
    command: 
      - /mnt/apiserver
